Index: rdma-core/ib2roce/pgm.c
===================================================================
--- rdma-core.orig/ib2roce/pgm.c
+++ rdma-core/ib2roce/pgm.c
@@ -63,6 +63,8 @@ struct pgm_tsi {
 	uint16_t sport;
 };
 
+enum { stream_init, stream_sync, stream_recovery, stream_unsync, stream_error } stream_status;
+
 /* Stream information */
 struct pgm_stream {
 	struct pgm_tsi tsi;
